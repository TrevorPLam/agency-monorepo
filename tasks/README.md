# Legacy Tasks Directory

This top-level `tasks/` directory is legacy planning material.

Use `docs/tasks/` as the canonical task system:

- `docs/tasks/README.md` is the canonical task index.
- `docs/tasks/1.md` is the canonical file-and-folder convention reference.

Do not add new task families under this legacy directory unless a migration task explicitly says to do so.